import { Routes } from '@angular/router';

import { VisitorListComponent } from './visitorList.component';

export const VisitorListRoutes: Routes = [
  { path: 'visitorList', component: VisitorListComponent }
];
